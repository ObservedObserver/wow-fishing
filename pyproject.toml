[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fishing"
version = "0.1.0"
description = "WoW fishing helper bot"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
